# Live generation walkthrough

This demo captures the real PoseForge Studio workflow. Cinematic zooms are centered on the selected character, pose, canvas, Generate button, completed image, History detail, and session-cost graph.

The real provider wait is captured but shortened during composition so the final walkthrough remains exactly 15 seconds.
